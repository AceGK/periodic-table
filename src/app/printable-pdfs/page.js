export const metadata = { title: "Printable PDFs — Elementable" };

export default function PrintablePdfsPage() {
  return (
    <main className="container" style={{ padding: "2rem 1rem" }}>
      <h1 style={{ fontSize: "1.5rem", marginBottom: "0.5rem" }}>Printable PDFs</h1>
      <p style={{ color: "var(--clr-text-secondary)", fontSize: "0.875rem" }}>Coming soon.</p>
    </main>
  );
}
